ex152:
Смоделировать слот машину (однорукий бандит). В котором имеется три барабана, на каждом барабане нарисованы 9 различных фруктов. 
Каждый запуск игры выдаёт случайным образом на каждом барабане в центре 1 фрукт. Машина должена быть представлена в виде объекта
с 3 свойствами массивами фруктов и одним методом получения случайного фрукта из каждого барабана.
let bandit = {
	cylinder1: ['яблоко', 'банан'..., 'апельсин'],
	cylinder2: ['яблоко', 'банан'..., 'апельсин'],
	cylinder3: ['яблоко', 'банан'..., 'апельсин'],
	roll(){
		let result = [];
		...
		return result;
	}
}


В итоге машина должна выдавать например 
['банан','апельсин','банан']. 

ex153:
Смоделировать игру со слот машиной.
Необходимо подсчитать кол-во запусков машины до получения выигрышной комбинации: ['банан','банан','банан'];
Ограничить кол-во запусков одним миллионом. 
result number или null (если за миллион запусков так и не выпадет выигрышная комбинация);


ex154:
Дан массив [1, 2, 3, 4, 5, 6, 7, 8, 9]
Необходимо пробежаться по массиву 3мя различными стандартными способами и подсчитать сумму всех элементов. 
Вообще основных способов 4. Если удасться 4мя то вообще супер. Этот способ мы пока не проходили, но возможно он уже встречался.

ex155:
Автобусные билеты в рулоне пронумерованы от 000001 до 999999.
Составить функцию, возвращающую объект с количеством и номерами всех счастливых билетов (массив) в некотором диапазоне (если передано start и end в функцию) или во всём рулоне если аргументов небыло.
Примечание: Счастливым считать тот билет, у которого сумма первых трех цифр равна сумме трех последних.
например: getLucky('011000', '011005') // {total: 1, tickets: ['011002']}

ex156:
Модуляция игры в города. 
Дан массив городов, см файл citys.txt. Взять из него массив. 
Два игрока. Предавлены в виде массива объектов
users = [
	{
		name: 'Василий',
		nextCity(chr){
		...
		return city;
		}
	},
	{
		name: 'Николай',
		nextCity(chr){
		...
		return city;
		}
	}
]

метод у каждого пользователя принимает букву, на которую он должен выбрать и вернуть город из массива городов. 
Игра должна начинается случайным образом (или первый или второй игрок, и он должен выбрать город на случайную букву).
Следующий игрок должен выбрать город на последнюю букву этого города который вернул предыдущий игрок.
Нельзя делать повторы. 
Если города кончаются буквами с которых нельзя начать слово например ь ы, то необходимо выбирать предыдущую ей букву.
Проигравшим считается тот, кто не сможет вернуть город.
В конце игра должна вернуть объект с именем победителя, и массивом городов которые называли оба игроков. 





