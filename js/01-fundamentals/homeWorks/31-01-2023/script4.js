'use strict';

alert('Программа переводящая температуру из градусов цельсия в форенгейта');

let temperature = Number(prompt('Температура в градусах цельсия'));

alert(temperature * 1.8 + 32 + ' Миль в час');
