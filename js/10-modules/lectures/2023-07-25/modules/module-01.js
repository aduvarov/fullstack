// Sum module

let v1 = `v1-888`;

console.log(v1);
function sum(x, y) {
    return x + y;
}

function doubleSum(x, y) {
    return (x + y) * 2;
}

let xxx = 777;

export { sum, doubleSum, xxx, v1 };
