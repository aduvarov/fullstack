'use strict';

//Написать функцию принимающую (катет1, катет2) прямоугольного треугольника и возвращющую его периметр.
let c1 = 6;
let c2 = 8;

function perimeter(c1, c2) {
    let p = Math.sqrt(c1 ** 2 + c2 ** 2) + c1 + c2;
    return p;
}
